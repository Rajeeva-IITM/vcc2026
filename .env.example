# Directory containing the input data files.
# Must contain the vcc_2026/ subdirectory (see README.md).
DATA_DIR=/path/to/your/data

# Path to this repository root (where results/ is written).
PROJECT=/path/to/vcc2026
